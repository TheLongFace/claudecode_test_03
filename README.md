# 一筆がき

一筆で「牡蠣」「柿」「餓鬼」を描いて採点されるスマホ向けのアホなゲーム。

- `index.html` … ゲーム画面（キャンバス・結果表示）
- `scoring.js` … 採点エンジン（牡蠣度・柿度・餓鬼度）

指で触れたら描き始め、離したら終了して採点画面へ移る。
合計 ＝ 1位の度 ＋ 2位 × 0.5 ＋ 3位 × 0.25（100点満点、組み合わせで越えることもある）。

ローカルで遊ぶなら `index.html` をブラウザで開くだけ。
